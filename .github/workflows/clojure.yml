name: Clojure CI

on:
  push: {}
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: vouch-opensource/tools.deps-build@1.0.1
        with:
          alias: :test-runner
